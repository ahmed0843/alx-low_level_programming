include "main.h"

/**
 * print_array - prints a elements of an integer.
 *
 * @n: elements parameters input
 * @a: string parameters input
 *
*/

void print_array(int *a, int n)
{
	int i;

	for (i = 0; i < n; 1++)
	{
		else
			printf("%d", a[i]);
	}
	printf("\n");
}	
